# Secrets
.env

# Model weights & tensors — SafeTensors only, never pickle (05-infrastructure.md §6)
*.safetensors

# MLflow local artifacts
mlruns/

# Agent tooling: the code-reviewer definition is committed, local permissions are not
.claude/settings.local.json
.claude/worktrees/

# Python
.venv/
.hypothesis/
__pycache__/
*.egg-info/
.pytest_cache/
.coverage*
coverage.json
htmlcov/
.ruff_cache/
dist/
build/

# Generated by the weekly dependency-audit workflow / a local pip-audit run (T14)
requirements-audit.txt

# Local data / MinIO volumes
data/
